import { describe, it } from "vitest"

describe("QuickAddTask", () => {
  describe("when the modal is opened", () => {
    it("displays the most recent tasks in a dropdown", async () => {
      // TODO
    })
    it("allows the user to select a task from the dropdown", async () => {
      // TODO
    })
  })
  describe("when the user selects a task", () => {
    it("is possible to create an entry when clicking the '+' button", async () => {
      // TODO
    })
  })
})
